# Environment variables

# Add the Anthropic API key in Vercel as ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=

# Notes:
# - Set this variable in Vercel Dashboard (Project → Settings → Environment Variables).
# - Recommended to add it for Preview and Production so preview deployments can call the chat API.
